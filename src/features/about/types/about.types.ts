export interface SkillCategory {
  title: string;
  subtitle: string;
  skills: string[];
}

export interface TimelineItem {
  period: string;
  title: string;
  organization: string;
  description: string;
}
